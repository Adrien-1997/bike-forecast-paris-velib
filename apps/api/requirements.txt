# =============================================================================
# Vélib' Forecast — Dépendances API (FastAPI + GCS + Pandas)
#
# Ce fichier liste les dépendances Python nécessaires à l’API :
# - stack FastAPI / Uvicorn / Starlette,
# - modèles Pydantic v2 + settings,
# - client GCS pour lire les artefacts (serving + monitoring),
# - stack data (pandas / numpy / pyarrow) pour manipuler les données.
#
# NB : les versions sont volontairement pinées pour garantir la stabilité
#      du déploiement (Cloud Run, Docker, etc.).
# =============================================================================

# ───────── Web API (FastAPI stack) ─────────
fastapi==0.115.4            # Framework web ASGI (routes, dépendances, OpenAPI)
uvicorn[standard]==0.30.6   # Serveur ASGI de prod (lancé dans le Dockerfile)
starlette==0.40.0           # Base ASGI utilisée par FastAPI (middleware, responses)

# ───────── Validation / configuration ─────────
pydantic==2.8.2             # Modèles de données (schemas de réponses, validation)
pydantic-settings==2.5.2    # Gestion des settings via BaseSettings (env, .env)

# ───────── Cloud / stockage ─────────
google-cloud-storage==2.18.2  # Client GCS (lecture JSON/parquet pour serving & monitoring)

# ───────── Temps / dates ─────────
python-dateutil==2.9.0.post0  # Parsing/arithmétique de dates (complément datetime)

# ───────── Data / numeric ─────────
pandas==2.2.2               # Manipulation de tableaux (DataFrame) côté API si besoin
pyarrow==16.1.0             # Backend parquet / Arrow (IO haute perf, compat pandas)
numpy==1.26.4               # Calcul numérique de base (arrays, stats légères)

# ───────── HTTP client ─────────
httpx==0.27.2               # Client HTTP async (appel Open-Meteo, éventuels services externes)
